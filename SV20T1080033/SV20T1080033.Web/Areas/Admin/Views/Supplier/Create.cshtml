@model SV20T1080033.DomainModels.Supplier;


<div class="box box-primary">
    <div class="box-body">
        <form action="~/Admin/Supplier/Save" method="post">
            @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
            {
                <div class="alert alert-warning">
                    <p>@ViewBag.ErrorMessage</p>
                </div>
            }
            <input type="hidden" name="@nameof(Model.SupplierID)" value="@Model.SupplierID" />
            <div class="form-group">
                <label>Tên Nhà cung cấp:</label>
                <input type="text" name="@nameof(Model.SupplierName)" value="@Model.SupplierName" class="form-control" autofocus>
            </div>
            <div class="form-group">
                <label>Tên giao dịch:</label>
                <input type="text" name="@nameof(Model.ContactName)" value="@Model.ContactName" class="form-control">
            </div>
            <div class="form-group">
                <label>Tỉnh thành:</label>
                <select class="form-control" name="@nameof(Model.Province)">
                    @foreach (var item in SV20T1080033.BusinessLayer.CommonDataService.ListOfProvinces())
                    {
                        if (item.ProvinceName == Model.Province)
                        {
                            <option value="@item.ProvinceName" selected>@item.ProvinceName</option>
                        }
                        else
                        {
                            <option value="@item.ProvinceName">@item.ProvinceName</option>
                        }
                    }
                </select>
            </div>
            <div class="form-group">
                <label>Địa chỉ:</label>
                <input type="text" name="@nameof(Model.Address)" value="@Model.Address" class="form-control">
            </div>
            <div class="form-group">
                <label>Số điện thoại:</label>
                <input type="number" name="@nameof(Model.Phone)" value="@Model.Phone" class="form-control">
            </div>
            <div class="form-group">
                <label>Địa chỉ Email:</label>
                <input type="email" name="@nameof(Model.Email)" value="@Model.Email" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">
                <i class="fa fa-floppy-o"></i> Lưu dữ liệu
            </button>
            <a href="~/Admin/Supplier/Index" class="btn btn-default">Quay lại</a>
        </form>
    </div>
</div>